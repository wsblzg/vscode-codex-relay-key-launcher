[project]
name = "vscode-codex-key-launcher"
version = "0.1.0"
description = "Tkinter launcher for opening isolated VSCode Codex windows from an accounts.json file."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]